# M3 阶段 5D：完整 UI 端到端验收

日期：2026-09-24（Asia/Shanghai）。仅使用 `test_meixin_m1.localhost` / `http://127.0.0.1:18081` 的现有 Edge、Playwright、Frappe 与 MariaDB；正式 `frontend` 未操作。

## 真实浏览器业务链

| 链 | Edge/Playwright 实际操作与断言 | 结果 |
| --- | --- | --- |
| A 建档至课消 | 原生表单新建虚构学生、课程、教师、教室和 20 课时产品；Scheduler 新购并分次付 `¥0.10 + ¥1.13`。`¥1.14` 超额被拒，修改后双击确认仅产生一次付款请求。满款后唯一 `购买授予 +20`；排课并提交“到课”执行单，详情显示 `M2 课消扣减 -1`、余额 19。 | 通过 |
| B 纠错与退款 | Manager 原生取消执行单，`M2 reversal 返还 +1`，余额 20；填写原因人工调整 `-1` 后余额 19；退款 `¥1.00` 关闭整包，`退款收回 -19`、余额 0、净收款 `¥0.23`；撤销退款后只恢复原收回快照 `+19`，净收款 `¥1.23`，原课消及返还流水均未改写。 | 通过 |
| C 赠送 | Manager 由列表创建赠送草稿：草稿无 Payment、无 Credit；原生提交后成交金额 `¥0.00`、唯一 `赠送授予 +20`、无虚假收款。 | 通过 |
| D 权限 | Scheduler 无赠送、退款和人工调整按钮；直接调用三个受控 API 均返回 403。Manager 直接调用 `frappe.client.insert` 写 Credit Entry 返回 403。User Permission 受限账户在课包报表及详情看不到目标包，直调概览 403；Guest 直调概览 403。 | 通过 |

再次使用全新夹具连续运行 A→B→C→D，并在同一隔离站完成下列补充：

| 补充边界 | 验证方式与结果 |
| --- | --- |
| 重复点击、超额 | 浏览器双击满款收款仅发一条 API 请求；`¥1.14` 对尚需 `¥1.13` 被拒，控制台出现 3 条**预期**业务拒绝事件，改为 `¥1.13` 后成功。退款 reversal 与人工调整的双击保护另见 5C-2 浏览器验收及完整回归。 |
| FEFO | 浏览器创建较晚激活、次日失效的赠送包；再次到课时先扣该包，其余额 20→19，较早激活但无失效日的购买包仍为 19。 |
| FIFO | 浏览器另建无失效日赠送包，并以已过期赠送包不可用的计划日期执行；原购买包更早激活，被扣 19→18，新赠送包仍为 20。 |
| 补款不重复 grant | 浏览器撤销原 `¥0.10` 收款后状态为欠费冻结、余额 18；Scheduler 补 `¥0.10` 后恢复生效、余额仍为 18，购买授予仍恰好一条。 |
| 余额不足、退款关闭后不得新扣课 | 本轮**未另以浏览器新建执行单复演**；隔离站完整自动化的 M3 `test_17`、`test_28`、`test_32`、`test_35`、`test_46` 覆盖无候选、耗尽、关闭及并发回滚。不得把这几项记作浏览器通过。 |
| DocShare 与故障回滚 | 未另作浏览器操作；完整自动化的 M3 `test_53`、`test_54`～`test_57` 及 M2 回滚用例覆盖。 |

## 控制台、回归与清理

- 浏览器正常业务步骤未出现未预期的 `error` / `pageerror`。预期拒绝单列：超额提交 3 条控制台事件；权限场景 4 个受控写入 API 返回 403，受限账户和 Guest 概览各返回 403。权限步骤记录 11 条控制台事件，其中 9 条对应预期 403，2 条为 Frappe 图标预加载警告。
- 多页面运行累计 26 条 Frappe 原生 `icons.svg` 预加载未使用警告；不记作零警告，也未把它们归为 M3 业务异常。浏览器没有调用正式站 URL。
- 首次在**浏览器夹具仍存在时**运行 141 项，8 项因旧用例采用全表计数断言而失败，实际多出的正是该批次课消、退款及调整流水。先精确清理该批次并恢复规则，再完整重跑：`Ran 141 tests in 36.752s / OK`，M1 30/30、M2 22/22、M3 89/89，0 失败、0 错误、0 跳过。没有修改或放宽旧断言。
- 5D 浏览器夹具清理：2 学生、1 课程、1 教师、1 教室、1 产品、3 学生课包、3 排课、3 执行、6 Payment、10 Credit、4 M2 Consumption、3 测试账号及 1 User Permission。清理后同一专用检查返回上述表及账号/权限全部为空。
- 五项课消规则在开始前均为“未配置”；验收结束按原快照显式恢复五项“未配置”，完整回归后再次只读核对仍为原值。
- 本阶段只增加可重复的隔离浏览器脚本、受站点守卫的夹具清理脚本和文档；未修改 M1/M2、M3 核心业务逻辑或正式数据库。正式 `frontend` 仍运行 `meixin-admin:m2-final-frappe16.34.0`。

## 待后续确认

5D 是隔离站 UI 验收，不代表正式站已部署或验收 M3。正式部署仍须按里程碑流程另做完整备份、变更影响与回滚说明，并取得用户确认。
