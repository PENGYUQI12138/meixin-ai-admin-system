# 美心行政系统 M3 正式发布记录

- 正式部署日期：2026-09-24（Asia/Shanghai）
- 生产源码：`1fe27cfdf13151df643f44beec670a8052f28c25`
- 生产标签：`prod-m3-2026-09-24`（指向上述源码提交）
- 正式 Docker 镜像 ID：`sha256:29e2c4abb30c52d65341b77e781de72122d29018ecb4a29a166e48b2afd688fb`
- 框架版本：Frappe 16.34.0、ERPNext 16.35.0

## 功能范围

学生购买或获赠课包、分次收款与退款关闭、不可变课时权益流水、剩余课时概览，以及与 M2 课消同事务扣减和撤销返还。现金、课时权益和课消事实分别记录；支持幂等、权限控制与并发保护。

## 验收与正式部署

- 隔离站完整回归 141/141 通过（M1 30、M2 22、M3 89）；Meixin CI V1 通过。
- 部署前完整备份目录名称：`meixin-m3-predeploy-20260924_153804`。数据库、公有文件、私有文件和站点配置同批备份，SHA256 校验通过；备份保存在 Git 外。
- 正式 `migrate` 仅执行一次，退出码 0；`clear-cache` 仅执行一次，退出码 0；正式入口已开放，未发生回滚。
- Schema：四张 M3 表和四个 DocType 已存在；四个金额列均为 `DECIMAL(21,2)`；六个要求的唯一索引均存在。
- 数据检查：M1/M2 九类记录部署前后均为 0；M3 四类记录均为 0。正式站未创建测试业务数据。

## 回滚基线与限制

正式回滚基线为 M2 final 源码 `615316ee991a172f198a378b2fa33db2ae60b000`、镜像 `sha256:35f61720d2d17494011b4d0bb23fcfa15fdc0704f37cc756191da32780a0b30b`，以及上述同批部署前完整备份。由于迁移改变了 schema，完整回退需要停写并成套恢复数据库与文件；仅切回镜像不足以恢复数据库。

当前限制：仅支持 CNY 两位小数、单一精确 Course 匹配、满款后一次性授予权益；退款关闭会收回剩余权益。不支持按比例部分退款后继续使用，也未包含教师工资、总账、会计凭证、支付渠道集成、家长端、发票、CRM、营销、AI 收费或大型经营报表。

M3 分支 `m3-enrollment-payment-package` 冻结保留。下一阶段 M4 必须从合并后的最新 `main` 新建独立分支。
