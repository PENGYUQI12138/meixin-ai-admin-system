# 下一步

1. 恢复浏览器控制通道后，按 `M2_ACCEPTANCE.md` 在隔离站完成 Scheduler / Manager 点击路径和控制台验收。
2. 浏览器门禁通过后形成 M2 候选稳定 checkpoint，再复核分支差异、隔离数据清理和正式容器状态。
3. 正式部署前对 `frontend` 做数据库、公私文件和站点配置完整备份，并验证备份可读；说明三个新 DocType、五项设置字段、索引、应用容器短时重建和回滚方案，等待用户明确确认。
4. 获确认后才可构建 M2 镜像、仅重建六个应用容器并对 `frontend` 执行一次 migrate；正式站不运行测试套件、不创建测试业务数据。

M2 不进入收费、课包余额、续费、教师工资、财务、微信、支付、AI 自动排课、大型经营报表或家长端。课消流水只是可审计决策，不是余额账户。
